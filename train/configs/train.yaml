DEVICE: cuda
RANDOM_SEED: 321

MODEL:
  NAME          : 'FastViT'
  NUM_CLASSES   : 2
  PRETRAINED    : True

DATASET:
  TRAIN_DATA_DIR: [
    "../dataset/catascan_data/train"
  ]
  NUM_WORKERS   : 16
  RATIO         : 0.8

TRAIN:
  BATCH_SIZE    : 64
  EPOCHS        : 100
  PATIENCE      : 20
  AMP           : True

LOSS:
  NAME: CrossEntropyLoss

OPTIMIZER:
  NAME: AdamW
  lr: 0.0001
  weight_decay: 0.0001

SCHEDULER:
  scheduler_name: 'ReduceLROnPlateau'
  mode: 'max'
  factor: 0.1
  patience: 5
  min_lr: 0.000001

SAVE_DIR        : 'outputs'

WANDB_PROJECT   : "cataract-detection"